class AppConstraints {
  static const double screenPadding = 12.0;
  static const double rowHeaderHeight = 44.0;
  static const double mapButtonMargin = 19.0;

  static const double inputContLeftPadding = 21.25;
  static const double inputContRightPadding = 15.0;
  static const double searchControlMargin = 20.0;

  static const double searchBarLeftPadding = 19.0;
  static const double searchBarRightPadding = 10.0;

  static const double searchBarReduceWidth = (screenPadding * 2 + searchBarLeftPadding + searchBarRightPadding + inputContLeftPadding + inputContRightPadding + searchControlMargin + 71);

  static const double suggestionsReduceWidth = (screenPadding * 2 + searchBarLeftPadding + inputContLeftPadding + inputContRightPadding + 5);
}
